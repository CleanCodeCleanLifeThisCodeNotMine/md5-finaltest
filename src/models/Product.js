class Product {
    constructor(id, name, importDate, quantity, productType) {
        this.id = id;
        this.name = name;
        this.importDate = importDate;
        this.quantity = quantity;
        this.productType = productType;
    }
}

export default Product;